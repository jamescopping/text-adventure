export const loadAdventureLog = () => {
    const log = document.createElement("div");
    log.id = "adventure-log";
    log.className = "text-light shadow p-3 mx-3 bg-dark border border-light rounded";
    return log;
}
